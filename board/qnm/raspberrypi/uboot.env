bootdelay=5
baudrate=115200

# Boot command cho RPi 4B (boot kernel từ FAT partition)
bootcmd=echo "Booting Linux from SD card..."; fatload mmc 0:1 \${kernel_addr_r} Image; booti \${kernel_addr_r} - \${fdt_addr}

# Environment mặc định
console=ttyAMA0,115200
